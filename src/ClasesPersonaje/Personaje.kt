package ClasesPersonaje

/**
 *
 * Clase principal de la que heredara cualquier tipo de Personaje
 *
 * @param nombre string que contiene el nombre
 * @param vivo boolean con el estado del personaje, true si esta vivo, false si esta muerto
 *
 * @author Diego
 *
 */
open class Personaje (var nombre: String, var vivo: Boolean)